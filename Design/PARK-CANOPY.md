# First PCG canopy pass

The saved park now contains 587 tree instances generated by an editable PCG graph. Ten retained stations come from individual OSM tree nodes; remaining stations are authored within mapped woodland, a lake-shore belt and the park boundary. These are not surveyed tree locations or a match to user photographs, which have not been supplied.

`Scripts/prepare_park_trees.py` deterministically constructs candidate stations in ESU using the existing geographic origin and scale. It excludes the water, path widths plus 180 cm, buildings, courts, pools, playgrounds, gardens, dog-park areas and the current frisbee clearings. Minimum spacing is 330 game cm. `Scripts/install_park_canopy.py` requires the converted park, traces every station onto Landscape, builds material bindings and creates a native PCG data asset/graph with a weighted static-mesh spawner. It verifies generated instance count before saving the map. It refuses to replace an existing canopy without review.

The interim tree mesh and seven textures come from the installed Epic Unreal 5.8 ArchVis template at `Templates/TemplateResources/Standard/ArchVis/Content/SampleScene/Tree`. Copied package paths are retained under `/Game/SampleScene/Tree`; rebuilt bark/leaf materials and PCG assets are under `/Game/BattleForTheA/Environment/Park`. Instanced-static-mesh material usage must be enabled before cooking. The original template material packages were not usable as copied and are not part of the authored canopy dependencies.

This is not the specified Megascans willow oak/magnolia/hardwood set. Species diversity, photo alignment, understory, grass, blossoms, leaves, wind and trunk collision remain unfinished. Canopy meshes deliberately have no collision; a broad leaf-canopy hull must never obstruct the path below it. Instancing and distance culling do not prove the 60 FPS requirement.


## Detailed canopy instancing review — 2026-09-12 [codex-maclaptop]

Added independent HISM review helper, leaving original PCG graph/data untouched. Saved PiedmontDetailedCanopyReview using migrated Hornbeam meshes, same existing stations and terrain placement. Conservative full-crown radius plus80 cm mapped-path margin retained only98/587 (77forest,21field); this is too aggressive for a convincing canopy and is not approved for main. Next clearance method should examine low branches/trunk at rider height, permitting safe higher overhangs rather than excluding entire crown footprints. Trunk collision and gameplay/performance remain pending.

Editor build and native placement succeeded. Static render required AllowCommandletRendering; corrected render-target variable collision and added image existence assertions. Last render produced lake/aerial images, but process exit1 was remote-control port conflict; use RCWebControlDisable for future launches. Images also show checkerboard pavement and missing water, so do not use this capture as whole-world material/quality proof. Lake camera now projects onto mapped path at native surface+170 cm. Main world/desktop048 unchanged; review not accepted.


## Rider-height canopy clearance — 2026-09-12 [codex-maclaptop]

Replaced full-crown exclusion with native LOD0 triangle inspection: every triangle entering the lower300 game-cm band contributes all its vertices to a conservative XY radius. Existing mapped path width plus80 cm clearance remains. This permits higher overhang while excluding low branches; terrain-relative differences between tree bases and nearby sloped paths still need native traversal/clearance verification. Missing mesh description fails closed.

Review now retains383/587 stations (335forest,48field), minimum low-band path margin80.231 cm. Editor build and placement pass; HISM tree is built synchronously before save. Render with AllowCommandletRendering and RCWebControlDisable exits0 and exports verified files. Inspected lakeshore frame: fuller canopy and open riding-height view, without earlier near-camera leaf wall. Static pavement/water appearance remains unsuitable for whole-scene acceptance; native runtime render and riding/performance required. Main and desktop048 unchanged, no trunk collision or PCG replacement yet.


## Native canopy runtime comparison uncovered map mismatch — 2026-09-12 [codex-maclaptop]

Added native three-camera capture and four-second wall-frame sampling after five-second warmup per view,1080p output, uncapped/VSync off, hidden stationary player and live world. Baseline main captured water and current trees: p50 11.279/12.421/13.698 ms. Detailed review captured finer thinner trees but NO lake water; its performance figures are not a valid isolated canopy comparison. RefreshWaterBody plus zone rebind/rebuild saved successfully but fresh native rerender still lacks water. No main/desktop changes.

Next use actual main world and transiently replace original canopy at runtime for comparison, avoiding SaveAs-derived water mismatch. Do not promote detailed review or claim60FPS based on these short mismatched runs. Native helper builds, captures complete; snapshots/JSON evidence retained including failed first water comparison. Review cameras are fixed geographic views, not full riding/path coverage; two shoreline views are off-path. Whole game remains unfinished.
